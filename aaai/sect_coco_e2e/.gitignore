# Python cache
__pycache__/
*.py[cod]
*.pyo

# Per-run diagnostics logs — large, regenerable, not version-controlled
*_diagnostics.jsonl
*.err
*.out
*.log

# Historical per-version result snapshots (regenerable, kept out of VCS)
unified_aptc_9datasets_v*.csv

# Seed-search trial dumps (regenerable)
seed42_search/

# Local experiment scratch
*.tmp
*.cache

# Local data root (datasets are not vendored)
data/

# Auto-sync working clone (kept outside project, ignored just in case)
.aaai2027-sync/
