这是一个我准备撰写论文的项目，7月26日AAAI2027截稿
所有的代码都要在虚拟环境aaai-e2e-subspace中运行
# AAAI2027 项目备忘

## 1. 项目概览

- 项目目录：`D:\study\研究牲\论文\AAAI2027`
- 当前目标：为 AAAI 2027 论文准备资料与实验基础。
- 截稿时间：根据现有说明，目标截稿时间是 `2026年7月26日`。
- 当前阶段判断：这个目录目前更像是“相关工作/基线方法收集与拆解仓库”，还不是完整的论文工程。现阶段主要包含两篇核心参考论文及其代码/抽取文本：
  - `GREET`：无监督图表示学习，重点处理边的同配/异配判别。
  - `ELSS`：属性图聚类，重点是显式低秩子空间学习与可扩展聚类。

## 2. 当前目录结构

### 顶层文件

- `GREET.pdf` / `GREET.extracted.txt` / `GREET.extracted.clean.txt`
  - GREET 论文原文与抽取文本。
- `ELSS.pdf` / `ELSS.extracted.txt` / `ELSS.extracted.clean.txt`
  - ELSS 论文原文与抽取文本。
- `AGENTS.md`
  - 当前仓库已有的操作说明，强调不要批量删除文件/目录，并说明这是 AAAI2027 论文项目，记录通读当前目录后得到的项目认知。


### 代码目录

- `GREET\code\GREET-main`
  - AAAI'23 论文 GREET 的主代码目录。
- `GREET\code\data`
  - GREET 运行时生成/缓存的数据，如结构编码 `se` 和 `knn` 图。
- `ELSS`
  - ELSS 的代码目录，包含模型、数据读取、评估脚本和数据文件。

## 3. 我对项目现状的判断

- 当前仓库里还没有看到你自己的论文草稿或主工程文件，例如：
  - 没有 `.tex` / `.docx` / `slides` / 实验日志 / 自己的方法实现。
  - 也没有统一的实验管理脚本、结果表格、配置文件目录。
- 因此，这个目录目前最像是：
  1. 收集候选相关工作；
  2. 保存论文 PDF 和抽取文本；
  3. 下载或整理基线代码；
  4. 为后续复现实验和提出新方法做准备。

## 4. GREET：当前已收集到的信息

### 4.1 论文基本信息

- 论文标题：`Beyond Smoothing: Unsupervised Graph Representation Learning with Edge Heterophily Discriminating`
- 会议：`AAAI 2023`
- 任务类型：无监督图表示学习（UGRL），并通过下游节点分类评估表示质量。

### 4.2 方法核心思想

从论文摘要和源码可以确认，GREET 的核心是：

- 认为传统无监督图表示学习过度依赖“同配性”假设。
- 图里不仅有同配边，也有异配边；如果一股脑沿所有边做平滑，会伤害表示质量，尤其是在异配图或含噪图中。
- GREET 引入一个 `Edge_Discriminator`，基于节点特征和结构信息，对边进行“同配/异配”软判别。
- 然后基于两类边构造双通道图视图，分别编码，再进行对比学习。
- 训练过程里，边判别与表示学习会互相促进。
提出了一种区分边异质性的无监督图表征学习框架：首先构建边判别器，结合节点特征和随机游走结构编码，通过最小化“锚定中心排序损失”无监督地预测每条边是同质边还是异质边；随后引入双通道图编码器，对同质边视图应用低通滤波（保留平滑信息），对异质边视图应用高通滤波（锐化特征）；最后利用鲁棒的跨通道对比损失交替训练边判别器和图编码器。
（GREET）的创新点：**1）首创无监督的边区分机制，无需标签即可通过设计的“锚定中心排序损失”有效分离同质边与异质边；2）提出低通与高通结合的双通道图编码架构，分别针对同质边和异质边提取低频共性和高频个性信号；3）设计了闭环的交替训练策略与InfoNCE对比损失，使边判别和表征学习在训练中相互促进、抵抗噪声。
（GREET）**的局限性在于：为了捕捉结构特征和进行对比学习，引入了随机游走结构编码和kNN节点搜索，虽然采用了局部敏感哈希（LSH）和Mini-batch优化，但预计算开销和内存占用仍远高于普通GNN，难以直接扩展到百万级节点的超大型图；此外，交替训练策略可能面临收敛稳定性和初始状态依赖的问题。

### 4.3 代码结构

主目录：`GREET\code\GREET-main`

关键文件：

- `main.py`
  - 训练主入口。
- `model.py`
  - 包含 `GCL`、`Edge_Discriminator`、`SGC` 等核心模块。
- `data_loader.py`
  - 数据集读取、结构编码生成、mask 处理。
- `utils.py`
  - 工具函数。
- `scripts\run_*.sh`
  - 针对不同数据集的运行脚本。

### 4.4 运行方式

README 中说明：直接运行对应数据集脚本即可，例如 `run_cora.sh`。

以 `scripts\run_cora.sh` 为例，实际调用的是：

```bash
python main.py -dataset cora -ntrials 10 -sparse 0 -epochs 400 -cl_batch_size 0 -nlayers_proj 1 -alpha 0.5 -k 20 -maskfeat_rate_1 0.8 -maskfeat_rate_2 0.1 -dropedge_rate_1 0.8 -dropedge_rate_2 0.8 -lr_disc 0.001 -margin_hom 0.5 -margin_het 0.5 -cl_rounds 2 -eval_freq 5
```

<!-- ### 4.5 依赖环境

`README.md` 中明确写了以下依赖：

- Python 3.9
- PyTorch 1.11.0
- PyTorch Geometric 2.0.4
- DGL 0.8.0
- NumPy 1.21.2
- scikit-learn 1.0.2
- SciPy 1.7.3 -->

### 4.6 支持的数据集

从 `main.py` 与 `data_loader.py` 可以确认，GREET 支持：

- `cora`
- `citeseer`
- `pubmed`
- `chameleon`
- `squirrel`
- `actor`
- `cornell`
- `texas`
- `wisconsin`
- `computers`
- `photo`
- `cs`
- `physics`
- `wikics`

### 4.7 数据与缓存习惯

从代码看，GREET 会额外生成两类缓存：

- 结构编码：`GREET\code\data\se\...`
- KNN 图：`GREET\code\data\knn\...`

这说明它不仅使用原始图结构，还会构造：

- 结构编码 `SE`
- 特征空间 KNN 正负样本掩码

### 4.8 当前目录里已经存在的数据痕迹

当前已看到：

- `GREET\code\data\se\cornell\cornell_16.pt`
- `GREET\code\data\se\cora\cora_16.pt`
- `GREET\code\data\knn\cornell\cornell_20.npz`
- `GREET\code\data\knn\cora\cora_20.npz`

说明至少 `cornell` 和 `cora` 相关的缓存已经生成过。

### 4.9 对本项目可能的启发

GREET 适合作为以下方向的参考：

- 异配图上的无监督表示学习；
- “不要默认所有边都该平滑”的研究思路；
- 边级别判别、结构去噪、双视图/双通道对比学习；
- 在 AAAI2027 选题中讨论“同配假设失效”或“噪声边/异配边处理”时，GREET 是很好的相关工作起点。

## 5. ELSS：当前已收集到的信息

### 5.1 论文基本信息

- 论文标题：`Beyond Implicit Constraint: Explicit Low-Rank Structured Subspace Learning for Fast Attributed Graph Clustering`
- 任务类型：属性图聚类（Attributed Graph Clustering, AGC）。（ELSS）**针对属性图聚类（AGC）任务，解决了现有子空间聚类方法依赖隐式低秩约束导致的时间/空间复杂度极高（无法扩展到大规模图）、难以捕捉复杂非线性流形结构，以及深层图神经网络（GNN）容易产生过度平滑的核心痛点
- 论文中方法名：`ELSS`

### 5.2 方法核心思想

从抽取文本和源码可总结为：
提出了一种显式低秩结构化子空间学习框架：首先通过同质性感知的自适应图滤波器处理节点特征，防止过度平滑；接着利用基于PageRank指导的结构化锚点采样策略，挑选具有全局拓扑重要性的节点；最后通过Nyström方法近似非线性核矩阵，在保持线性复杂度的同时构建显式低秩正交基，并通过后处理进行K-Means聚类。

- 关注属性图聚类，而不是节点分类。
- 方法强调“显式低秩结构化子空间学习”，而不是只依赖隐式约束。
- 通过图滤波缓解过平滑问题，并保留有判别性的自信息。
- 使用 PageRank 引导的锚点采样做近似，从而追求线性可扩展性。
1）提出了非线性显式低秩表征框架，突破了现有可扩展子空间聚类仅限于线性核的表达瓶颈；2）设计了基于局部同质性动态调节平滑强度的自适应图滤波器，有效保留了自我鉴别信息并克服了过度平滑；3）引入了PageRank指导的结构化采样策略，替代了传统的均匀采样，显著提升了Nyström近似的准确性和在大规模图上的线性扩展能力。


### 5.3 代码结构

主目录：`ELSS`

关键文件：

- `demo.py`
  - 主运行入口。
- `model.py`
  - 核心模型 `RobustGraphSubClustering`。
- `data_load.py`
  - 数据加载、归一化、稀疏矩阵转张量、聚类准确率计算。
- `evaluate.py`
  - 聚类指标：`NMI`、`ARI`、`ACC`。
- `utils_cym.py`
  - 随机种子与确定性设置。

### 5.4 支持的数据集

从 `data_load.py` 和实际文件可以确认，当前项目中 ELSS 支持或已准备的数据包括：

- `acm`
- `dblp`
- `pubmed`
- `wiki`
- `computers`
- `arxiv`（代码里对应 `ogbn-arxiv`）

其中本地已存在：

- `ELSS\data\acm.mat`
- `ELSS\data\dblp.mat`
- `ELSS\data\pubmed.mat`
- `ELSS\data\wiki.mat`
- `ELSS\data\computers.mat`
- `ELSS\data\ogbn_arxiv\...`

### 5.5 运行方式

`demo.py` 的参数非常少，主要是：

- `--dataset`
- `--power`

默认调用示意：

```bash
python demo.py --dataset dblp --power 2
```

### 5.6 代码层面的观察

ELSS 代码里有几个值得后续注意的点：

1. `demo.py` 解析了 `--power`，但真正实例化模型时写的是 `power=20`，并没有使用 `args.power`。
   - 这意味着当前脚本参数和实际运行逻辑并不完全一致。

2. `demo.py` 里有：

```python
for i in np.arange(1, 2, 1):
```

   - 这个循环实际上只会跑一次，像是实验脚本残留。

3. `evaluate.py` 里的类型注解写成了返回两个值，但实际返回了三个指标：
   - `NMI`
   - `ARI`
   - `ACC`

这些问题不一定影响“先读懂代码”，但如果后面复现 ELSS，建议优先核查。

### 5.7 模型实现线索

从 `model.py` 看，ELSS 的实现包含几个关键机制：

- 图卷积/图滤波：`convolve`
- 自适应滤波系数：`filter_coef`
- 稀疏图上的 PageRank：`compute_pagerank`
- Nyström 近似：`nystrom_approx_top_k`
- 锚点子空间求解：`solve_irls`
- 最终通过 `KMeans` 做聚类

也就是说，这份实现不是简单的 GNN 编码器，而是更偏：

- 图滤波
- 低秩近似
- 锚点采样
- 谱/子空间聚类

### 5.8 对本项目可能的启发

ELSS 适合作为以下方向的参考：

- 大规模属性图聚类；
- 显式低秩建模；
- 锚点近似与可扩展无监督图学习；
- 如何在无监督图任务中平衡结构传播和节点自信息保留。

## 6.1 两条参考线索之间的关系

当前目录里的两篇工作虽然任务不完全相同，但有明显共通主题：

- 都属于无监督图学习范畴。
- 都在对抗“简单图平滑不够好”这个问题。
- 都强调图结构并不总是可靠，需要更精细地处理：
  - GREET：从“边类型”出发，区分同配边/异配边；
  - ELSS：从“传播强度与子空间结构”出发，避免过平滑并提升聚类质量。

如果后续 AAAI2027 的论文方向仍围绕无监督图学习，那么可以考虑的桥接问题包括：

- 异配感知的属性图聚类；
<!-- - 面向聚类任务的边级别筛选/重加权； -->
- 将显式低秩子空间学习与异配边判别结合；
<!-- - 在大规模图上做结构去噪与高效无监督学习。 -->

## 6.1 两条参考线索之间的关系
两篇论文创新点对照分析：
（1）同质性与图滤波机制（创新点类别：结构/特征提取）
ELSS提出“同质性感知自适应滤波器”，通过节点间余弦相似度动态调节平滑强度（软权重）以防止过度平滑；GREET采用“无监督边判别与双通道编码”，利用判别器区分同质/异质边，并分别施加低通（平滑）与高通（锐化）滤波器。二者可融合之处在于：用GREET的高/低通双通道滤波机制替代ELSS单一自适应滤波器，以提取更丰富的特征。但潜在冲突是：ELSS的“自适应软权重调节”和GREET的“边硬/软切分双通道”在功能上存在重叠，同时使用可能造成特征提取冗余。若融合成功，潜在收益是可同时捕捉低频共性与高频个性信号，并在极端异质图或对抗攻击场景下获得更鲁棒的特征；潜在风险则是模型复杂度上升，削弱ELSS原本极简高效的图滤波流程并增加前向传播耗时。
（2）目标函数与学习范式（创新点类别：损失函数/训练策略）
ELSS强调“非线性显式低秩子空间学习”，摒弃隐式核范数，将节点映射到RKHS空间并直接求解正交基，目标指向无监督聚类；GREET采用“双通道对比学习与交替训练”，以InfoNCE损失拉近节点同/异质视图表征，目标更偏向通用节点表征学习（如节点分类等）。可融合的方式是：先用GREET交替训练获得高质量表征，再将其输入ELSS的显式低秩近似模块完成无监督聚类。潜在冲突来自空间几何假设差异：对比学习（InfoNCE）倾向于使特征在超球面上更均匀分布，而子空间学习假设数据位于低秩线性子空间的并集之上。若融合有效，潜在收益是贯通“预训练表征提取”到“下游高效聚类”的全链路，使聚类边界更清晰并有望显著提升ACC、NMI等指标；潜在风险是两类目标的优化方向可能不一致，联合训练时难以平衡对比损失与低秩重构损失的权重。
## 7. 当前仓库的空白点

目前还没有看到以下内容：

- 你的论文题目或摘要草稿；
- 自己的方法设计文档；
- 自己的实验脚本；
- 统一的环境配置文件（如 `requirements.txt`、`environment.yml`）；
- 结果表格、日志、可视化输出；
- 论文写作目录（如 `paper/`、`draft/`、`tex/`）。

这意味着后续很可能需要逐步补齐：

1. 文献综述与问题定义；
2. 基线复现记录；
3. 自己的方法原型；
4. 实验与写作材料的统一组织。

## 8. 我建议后续优先补的东西

如果这个目录会继续长期使用，建议尽快补充以下结构：

1. `notes/`
   - 记录每篇论文的关键点、优缺点、可借鉴想法。

2. `experiments/`
   - 保存运行命令、参数、结果和日志。

3. `paper/` 或 `draft/`
   - 放论文草稿、图表、参考文献。

4. `baselines/` 或统一说明文件
   - 说明每个基线的来源、依赖、如何运行、是否已复现。

## 9. 一句话总结

这个项目当前已经具备了两个很有价值的参考起点：

- `GREET` 偏“异配感知的无监督图表示学习”；
- `ELSS` 偏“可扩展的显式低秩属性图聚类”。

如果 AAAI2027 的选题还在无监督图学习、图聚类、异配图、去噪或抗过平滑这一带，这两个基线都值得继续深挖，而且它们之间也存在自然的结合空间。

